<!DOCTYPE html>
<html lang="en" dir="ltr">

<head>
  <meta charset="utf-8">
  <title>To Do List</title>
</head>

<body>

  <%  if (kinfOfDay === "Saturday" || kinfOfDay === "Sunday") { %>
  <h1 style="color: purple">
    <%= kinfOfDay %> List</h1>
  <% } else { %>
    <h1 style="color: blue"><%= kinfOfDay %> ToDo List</h1>
  <% } %>




</body>

</html>
